<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>FLX CLOUD DRIVE - Gallery</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h1>🖼️ FLX CLOUD DRIVE Gallery</h1>

    <p class="hint"><a href="/">← Back to Upload</a></p>

    <div class="upload-list">
      <% if (files && files.length > 0) { %>
        <% files.forEach(file => { %>
          <div>
            <img src="<%= file.url %>" alt="<%= file.name %>" />
            <p><%= file.name %></p>
          </div>
        <% }) %>
      <% } else { %>
        <p class="hint">No files yet. <a href="/">Upload one now!</a></p>
      <% } %>
    </div>
  </body>
</html>
