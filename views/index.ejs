<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>FLX CLOUD DRIVE</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h1>☁️ FLX CLOUD DRIVE</h1>

    <form action="/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="file" accept="image/*" required />
      <button type="submit">Upload to Drive</button>
    </form>

    <% if (!flags || flags.enableGallery !== false) { %>
      <p class="hint">Go to <a href="/gallery">Gallery</a> to view your uploads.</p>
    <% } %>
  </body>
</html>
